#   Array

An array is a data type that represents a collection of objects where each can be selected by an index.


### Example

```json
[
    "Hello",
    "world!"
]
```

This example contains two [string][string] elements: `"Hello"` and `"world!"`.
